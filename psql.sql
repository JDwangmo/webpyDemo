#!/usr/bin/env bash


CREATE TABLE yichutang_log(username VARCHAR(80), qq VARCHAR(80),
   product VARCHAR(80), guest VARCHAR(80), s_province VARCHAR(80),
   mobile VARCHAR(80), s_county VARCHAR(80), address VARCHAR(80) , s_city VARCHAR(80),
   date DATE
   )
